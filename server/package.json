{
  "name": "realmehkem-json-server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT --watch db.json --cors"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
